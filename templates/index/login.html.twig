{% extends '/base.html.twig' %}

{% block title %} Login {% endblock %}

{% block content %}
        <div class="container d-flex justify-content-center h-25">
            <img class="loginImg" src="{{ asset('img/loginImg.jpg') }}" alt="loginImg" style="height: 33rem">
        </div>
{% endblock %}
{% block content2 %}
    <div class="container mt-5 d-flex justify-content-center text-light">
        {{ form(loginForm) }}
    </div>

{% endblock %}